# This is a Databricks asset bundle definition for demo_dabs_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: demo_dabs_project

include:
  - resources/*.yml

variables:
  dbt_catalog:
    description: Catalog to use for DBT
    default: development-catalog
  dbt_schema:
    description: Schema to use for DBT
    default: dbt-schema
  dbt_warehouse:
    description: SQL Warehouse running the DBT SQL commands.
    default: dd2a70114bc8b34a
    lookup:
      warehouse: 'ABC Warehouse'
  warehouse_id:
    description: SQL Warehouse running dashboard
    lookup:
      warehouse: 'ABC Warehouse'
  dbt_data_notebook_path: 
    description: Path to the notebook that creates the data for DBT


targets:
  # The 'dev' target, for development purposes. This is the default target in the project. ADDING TEXT 
    
  dev:
    # We use 'mode: development' to indicate this is a personal development copy:
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default
    # - The 'development' mode is used for Delta Live Tables pipelines
    mode: development
    default: true
    variables:
      dbt_catalog: development_catalog
      dbt_schema: dbt-schema
      #dbt_warehouse: b400fd8ff0fbe95b
      #warehouse_id: b400fd8ff0fbe95b
      dbt_data_notebook_path: /Workspace/Users/kamil.nowinski@cloudformations.org/.bundle/demo_dabs_project/dev/files/src/dbt_data
    workspace:
      host: https://adb-7405614498669357.17.azuredatabricks.net/

  ## Optionally, there could be a 'staging' target here.
  ## (See Databricks docs on CI/CD at https://docs.databricks.com/dev-tools/bundles/index.html. 
  #
  # staging:
  #   workspace:
  #     host: https://adb-1728909964136821.1.azuredatabricks.net

  # The 'prod' target, used for production deployment.
  prod:
    # We use 'mode: production' to indicate this is a production deployment. 
    # Doing so enables strict verification of the settings below.
    mode: production
    variables:
      dbt_catalog: production_catalog
      dbt_schema: dbt-schema
      #dbt_warehouse: 740e74a406d8fbf2
      #warehouse_id: 740e74a406d8fbf2
      dbt_data_notebook_path: /Workspace/Users/kamil.nowinski@cloudformations.org/.bundle/demo_dabs_project/prod/files/src/dbt_data
    workspace:
      host: https://adb-7405614498669357.17.azuredatabricks.net/
      # We always use /Users/kamil.nowinski@cloudformations.org for all resources to make sure we only have a single copy.
      # If this path results in an error, please make sure you have a recent version of the CLI installed.
      root_path: /Users/kamil.nowinski@cloudformations.org/.bundle/${bundle.name}/${bundle.target}
    run_as:
      # This runs as kamil.nowinski@cloudformations.org in production. We could also use a service principal here,
      # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
      user_name: kamil.nowinski@cloudformations.org
